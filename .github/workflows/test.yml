name: new workflow

on: [push]

jobs:
  run-shell:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "hello world"
      - name: multiline scripts
        run: |
          node -v
          npm -v